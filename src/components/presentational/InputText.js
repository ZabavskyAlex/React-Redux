import React, {Fragment} from "react";

const InputText = () => {
    return(
            <Fragment>
                <input type="text"/>
            </Fragment>
        )
};

export default InputText;